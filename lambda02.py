#!/usr/bin/python


# lambda functions are one-off/custom functions that are used solely for a custom purpose
# lambda can be put inline so they are close to
g = lambda x: x + 1


if __name__ == '__main__':
    g(3)
